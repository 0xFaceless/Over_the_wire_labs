# Bandit Level 30 → Level 31

## Goal

Find the password stored in a Git tag.

### Commands Used

```bash
git tag
git show secret
```

### Concepts Learned

- Git Tags
- Listing tags
- Viewing tag contents using `git show`

### Result

The password was stored inside the `secret` Git tag.