# Bandit Level 7 → 8

## Objective

Retrieve the password for Bandit Level 8. The password is stored next to the word `millionth` in the file `data.txt`.

**Username:** `bandit7`

## Skills Learned

- Searching text with `grep`
- Counting lines with `wc`
- Working with large text files

## Commands Used

```bash
ssh
ls
wc
grep
```

## Key Takeaways

- `wc -l` counts the number of lines in a file.
- `grep` searches for text that matches a given pattern.
- `grep` is much faster than manually searching through large files.

## Blog

📖 Read the complete walkthrough:

[Walkthrough](https://0xfaceless.hashnode.dev/overthewire-bandit-level-7-8-walkthrough)